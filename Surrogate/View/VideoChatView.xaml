<UserControl x:Class="Surrogate.View.VideoChatView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:OpenTok="clr-namespace:OpenTok;assembly=WPFVideoRenderer"
        xmlns:local="clr-namespace:Surrogate.View"
        mc:Ignorable="d">
    <DockPanel>
        <StackPanel DockPanel.Dock="Left">
            <Label Content="Kontakte" FontWeight="Bold"/>
            <ListView >
            </ListView>
        </StackPanel>
        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="Videochatgespräch mit:" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
        </StackPanel>
        <StackPanel>
            <OpenTok:VideoRenderer x:Name="PublisherVideo" HorizontalAlignment="Right" Height="240" VerticalAlignment="Top" Width="320">
            </OpenTok:VideoRenderer>
            <OpenTok:VideoRenderer x:Name="SubscriberVideo" HorizontalAlignment="Center" Height="480" VerticalAlignment="Bottom" Width="640">
            </OpenTok:VideoRenderer>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Anrufen"></Button>
                <Button Content="Auflegen"></Button>
            </StackPanel>
        </StackPanel>
    </DockPanel>

</UserControl>
